import { makeStyles } from '@material-ui/core';

export const bottomBarStyles = makeStyles(() => ({
  root: {
    width: '100%',
    height: '5em',
    background: 'white',
    top: 'auto',
    bottom: 0,
  },
}));
