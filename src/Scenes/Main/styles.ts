import { makeStyles } from '@material-ui/core';

export const mainStyles = makeStyles(() => ({
  root: {
    width: '100%',
    height: '80vh',
    display: 'flex',
    flexDirection: 'column',
  },
}));
